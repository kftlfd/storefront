{
  "name": "storefront",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "prettier": "yarn workspaces run prettier"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
